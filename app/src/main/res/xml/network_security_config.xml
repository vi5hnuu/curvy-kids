<?xml version="1.0" encoding="utf-8"?>
<!--
    ML Kit model downloads use HTTPS, so no cleartext exception is needed.
    The old 192.168.176.77 entry was for the dev WebView server and has been removed.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
